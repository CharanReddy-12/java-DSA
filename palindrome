class Solution {
    public static boolean isPalindrome(int x) {
        int r = 0;
        int o = x;
        while (x > 0) {
        int digit = x % 10;
        r = r * 10 + digit;
        x /= 10;
}
return o==r;
    }
}
